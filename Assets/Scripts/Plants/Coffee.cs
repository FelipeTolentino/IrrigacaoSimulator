using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Coffee : Plant
{
    // Start is called before the first frame update
    protected override void Start() {
        displayName = "Café";
        base.Start();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
